@model IEnumerable<skabi.web.mvc.Models.carbrand> 
@{
    ViewBag.Title = "Inredningsförslag";
}
@section AddToHead{

    <link href="@Url.Content("~/Content/proposals.css")" rel="stylesheet" type="text/css" />

}
@section JavaScript{
    <script src="@Url.Content("~/scripts/proposals.js")"  type="text/javascript"></script>
}
<script type="text/javascript">

</script>
<div id="proposalsMenu">
    <table style="float: left;">
        <tr>
            <td>
                <div id="carbrands" class="menuColumn" action="GetCarModels">
                    <table>
                        @foreach (var item in Model)
                        {
                            <tr>
                                <td>
                                    <div class="proposalMenuLinks menuitem" id="@item.id">
                                        @item.brand_name
                                    </div>
                                </td>
                            </tr>
                        }
                    </table>
                </div>
            </td>
            <td>
                <div id="carmodels" class="menuColumn borderleft" action="GetCarModelTypes">
                    <table>
                    </table>
                </div>
            </td>
            <td>
                <div id="carmodeltypes" class="menuColumn borderleft">
                    <table>
                    </table>
                </div>
            </td>
        </tr>
    </table>
</div>
@*<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table>
    <tr>
        <th>
            carmodeltype_id
        </th>
        <th>
            proposal_id
        </th>
        <th>
            left_right
        </th>
        <th>
            uniqueclicks
        </th>
        <th>
            order_id
        </th>
        <th>
        </th>
    </tr>
    @foreach (var item in Model)
    {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.carmodeltype_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.proposal_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.left_right)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.uniqueclicks)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.order_id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.carmodeltype.carmodel.carbrand.brand_name)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.id }) |
                @Html.ActionLink("Details", "Details", new { id = item.id }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.id })
            </td>
        </tr>
    }
</table>*@
